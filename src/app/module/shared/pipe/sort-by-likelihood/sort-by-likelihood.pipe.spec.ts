import { SortByLikelihoodPipe } from './sort-by-likelihood.pipe';

describe('SortByLikelihoodPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByLikelihoodPipe();
    expect(pipe).toBeTruthy();
  });
});
